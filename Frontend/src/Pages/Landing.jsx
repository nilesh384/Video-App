import React, { useEffect, useRef } from "react";

import AOS from "aos";
import "aos/dist/aos.css";


const Landing = () => {
  const typedElement = useRef(null);

  return (
    <div className="h-screen">Landing page</div>
  );
};

export default Landing;
